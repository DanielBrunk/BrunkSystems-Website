---
import Layout from '../../components/Layout.astro';
import ContactTile from '../../components/ContactTile.astro';
---

<Layout title="Webentwicklung – BrunkSystems" description="Moderne Websites und Webanwendungen. Astro, SvelteKit, WordPress — schnell, sicher und auf den Punkt.">
  <section class="tiles">
    <div class="tile tile--primary">
      <a href="/" class="back">&larr; Startseite</a>
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polyline points="16 18 22 12 16 6" />
        <polyline points="8 6 2 12 8 18" />
      </svg>
      <h1>Webentwicklung</h1>
      <p class="tile__subtitle">Moderne Websites und Webanwendungen — schnell, sicher und auf den Punkt.</p>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="m6 9 6 6 6-6" />
      </svg>
      <h2>Statische Websites</h2>
      <p>
        Mit Astro bauen wir blitzschnelle, statische Websites. Kein unnötiger JavaScript-Ballast,
        perfekte Lighthouse-Scores und minimale Ladezeiten. Ideal für Firmenseiten,
        Landingpages und Portfolios.
      </p>
    </div>

    <div class="tile tile--alt">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <rect width="18" height="18" x="3" y="3" rx="2" />
        <path d="M3 9h18" />
        <path d="M9 21V9" />
      </svg>
      <h2>Web-Applikationen</h2>
      <p>
        Komplexere Anforderungen? Mit SvelteKit entwickeln wir interaktive Dashboards,
        interne Tools und datengetriebene Anwendungen. Reaktiv, modern und wartbar.
      </p>
    </div>

    <div class="tile tile--secondary">
      <h2>Technologien</h2>
      <ul class="tile__feature-list tile__feature-list--light">
        <li>Astro — Statische Sites, maximale Performance</li>
        <li>SvelteKit — Interaktive Webanwendungen</li>
        <li>WordPress — Wenn ein CMS gebraucht wird</li>
        <li>TypeScript — Typsichere Entwicklung</li>
        <li>Reines CSS — Kein Framework-Overhead</li>
        <li>Supabase — Backend as a Service</li>
      </ul>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M12 20h9" />
        <path d="M16.376 3.622a1 1 0 0 1 3.002 3.002L7.368 18.635a2 2 0 0 1-.855.506l-2.872.838a.5.5 0 0 1-.62-.62l.838-2.872a2 2 0 0 1 .506-.854z" />
      </svg>
      <h2>Statisch vs. Dynamisch</h2>
      <p>
        Nicht jedes Projekt braucht ein CMS. Wir beraten ehrlich, welche Lösung
        für Ihre Anforderungen die richtige ist — und setzen genau das um.
        Einfache Firmenseite? Statisch. Regelmäßig neue Inhalte? CMS. Interne Tools? Web-App.
      </p>
    </div>

    <div class="tile tile--alt">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M21 12a9 9 0 0 1-9 9m9-9a9 9 0 0 0-9-9m9 9H3m9 9a9 9 0 0 1-9-9m9 9c1.66 0 3-4.03 3-9s-1.34-9-3-9m0 18c-1.66 0-3-4.03-3-9s1.34-9 3-9m-9 9a9 9 0 0 1 9-9" />
      </svg>
      <h2>DSGVO-konform</h2>
      <p>
        Alle unsere Websites sind von Grund auf datenschutzkonform. Keine Cookies,
        kein Tracking, kein Cookie-Banner nötig. Hosting auf europäischen Servern,
        lokale Fonts, keine externen Abhängigkeiten.
      </p>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M13 2 3 14h9l-1 8 10-12h-9l1-8z" />
      </svg>
      <h2>Performance</h2>
      <p>
        Lighthouse 100 ist kein Zufall, sondern unser Standard. Optimierte Bilder,
        minimales CSS, keine Render-Blocker. Ihre Website lädt in unter einer Sekunde —
        auf jedem Gerät.
      </p>
    </div>

    <div class="tile tile--dark">
      <ContactTile />
    </div>
  </section>
</Layout>

<style>
  .tiles {
    display: grid;
    grid-template-columns: 1fr 1fr;
    max-width: 1200px;
    margin-inline: auto;
  }

  .tile {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 50vh;
    padding: var(--space-2xl) var(--space-3xl);
    color: var(--color-text);
    background: var(--color-background);
    border-bottom: 1px solid var(--color-border);
  }

  .tile:nth-child(odd) {
    border-right: 1px solid var(--color-border);
  }

  .tile--primary {
    background: var(--color-primary);
    color: #FFFFFF;
    border-color: rgba(0, 0, 0, 0.08);
  }

  .tile--alt {
    background: var(--color-background-alt);
  }

  .tile--secondary {
    background: var(--color-secondary);
    color: var(--color-text);
    border-color: rgba(0, 0, 0, 0.1);
  }

  .tile--dark {
    background: var(--color-text);
    color: var(--color-text-inverse);
    border-color: rgba(255, 255, 255, 0.1);
  }

  .tile__icon {
    width: 48px;
    height: 48px;
    margin-bottom: var(--space-lg);
    opacity: 0.7;
  }

  .tile h1 {
    font-family: var(--font-family-heading);
    font-size: var(--font-size-3xl);
    line-height: var(--line-height-tight);
    margin-bottom: var(--space-md);
  }

  .tile__subtitle {
    font-size: var(--font-size-md);
    opacity: 0.75;
  }

  .tile h2 {
    font-family: var(--font-family-heading);
    font-size: var(--font-size-2xl);
    margin-bottom: var(--space-md);
  }

  .tile p {
    color: var(--color-text-light);
    max-width: 45ch;
  }

  .tile--primary p,
  .tile--primary .tile__subtitle {
    color: rgba(255, 255, 255, 0.8);
  }

  .tile--secondary h2 {
    color: var(--color-text);
  }

  .tile--dark p {
    color: rgba(234, 239, 239, 0.75);
  }

  .tile__feature-list {
    list-style: none;
    padding: 0;
  }

  .tile__feature-list li {
    padding: var(--space-sm) 0;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    color: var(--color-text);
  }

  .tile__feature-list--light li {
    border-color: rgba(0, 0, 0, 0.1);
  }

  .tile__feature-list li:last-child {
    border-bottom: none;
  }

  .back {
    font-size: var(--font-size-sm);
    color: rgba(255, 255, 255, 0.7);
    text-decoration: none;
    margin-bottom: var(--space-xl);
  }

  .back:hover {
    color: #FFFFFF;
  }

  @media (max-width: 639px) {
    .tiles {
      grid-template-columns: 1fr;
    }

    .tile {
      min-height: 45vh;
      padding: var(--space-xl) var(--space-lg);
      border-right: none;
    }

    .tile:nth-child(odd) {
      border-right: none;
    }

    .tile h1 {
      font-size: var(--font-size-2xl);
    }
  }
</style>
