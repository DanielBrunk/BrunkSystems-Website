---
import Layout from '../../components/Layout.astro';
import ContactTile from '../../components/ContactTile.astro';
---

<Layout title="SmartGarden – BrunkSystems" description="Intelligente Gartenbewässerung mit IoT-Sensoren. LoRaWAN, Hydrawise-Integration und automatische Steuerung.">
  <section class="tiles">
    <div class="tile tile--primary">
      <a href="/" class="back">&larr; Startseite</a>
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 4.18 2 8 0 5.5-4.78 10-10 10Z" />
        <path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12" />
      </svg>
      <h1>SmartGarden</h1>
      <p class="tile__subtitle">Intelligente Gartenbewässerung. Sensoren messen, das System steuert - Ihr Garten profitiert.</p>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M5 12.55a11 11 0 0 1 14.08 0" />
        <path d="M1.42 9a16 16 0 0 1 21.16 0" />
        <path d="M8.53 16.11a6 6 0 0 1 6.95 0" />
        <line x1="12" x2="12.01" y1="20" y2="20" />
      </svg>
      <h2>LoRaWAN-Sensoren</h2>
      <p>
        Bodenfeuchtigkeit, Temperatur, Niederschlag - unsere Sensoren erfassen
        die relevanten Daten in Ihrem Garten. Funkbasiert, stromsparend und
        wartungsarm mit Reichweiten von mehreren Kilometern.
      </p>
    </div>

    <div class="tile tile--alt">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.1 3 12.25c0 2.22 1.8 4.05 4 4.05z" />
        <path d="M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 2.5 2 4.9 4 6.5s3 3.5 3 5.5a6.98 6.98 0 0 1-11.91 4.97" />
      </svg>
      <h2>Bewässerungssteuerung</h2>
      <p>
        Integration mit Hunter Hydrawise und anderen Bewässerungssystemen.
        Die Sensordaten steuern automatisch, wann und wie viel bewässert wird.
        Wasser sparen, Pflanzen schützen.
      </p>
    </div>

    <div class="tile tile--secondary">
      <h2>So funktioniert es</h2>
      <dl class="tile__dl">
        <dt>1. Sensoren installieren</dt>
        <dd>LoRaWAN-Sensoren an den richtigen Stellen im Garten platzieren.</dd>
        <dt>2. Daten erfassen</dt>
        <dd>Die Sensoren messen kontinuierlich und senden die Daten an die Plattform.</dd>
        <dt>3. Automatisch steuern</dt>
        <dd>Das System entscheidet anhand der Daten, ob und wann bewässert wird.</dd>
        <dt>4. Auswerten</dt>
        <dd>Dashboard mit Verlaufsdaten, Verbrauch und Zustand Ihres Gartens.</dd>
      </dl>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <ellipse cx="12" cy="5" rx="9" ry="3" />
        <path d="M3 5V19A9 3 0 0 0 21 19V5" />
        <path d="M3 12A9 3 0 0 0 21 12" />
      </svg>
      <h2>Datenplattform</h2>
      <p>
        Alle Sensordaten fließen in eine zentrale Plattform. Verlaufswerte,
        Trends und Auswertungen - übersichtlich auf Ihrem Gerät.
        Ihre Daten bleiben bei Ihnen, kein Cloud-Zwang.
      </p>
    </div>

    <div class="tile tile--alt">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" />
        <circle cx="9" cy="7" r="4" />
        <line x1="19" x2="19" y1="8" y2="14" />
        <line x1="22" x2="16" y1="11" y2="11" />
      </svg>
      <h2>Für wen?</h2>
      <p>
        Privatanwender mit Garten und Bewässerungssystem, die Wasser sparen
        und ihren Garten datenbasiert pflegen wollen. Auch für GaLaBau-Betriebe,
        Kommunen und Facility Management.
      </p>
    </div>

    <div class="tile">
      <svg class="tile__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10" />
        <path d="m9 12 2 2 4-4" />
      </svg>
      <h2>Aus der Praxis</h2>
      <p>
        SmartGarden entstand im eigenen GaLaBau-Betrieb. Was wir unseren
        Kunden anbieten, nutzen wir selbst - täglich, bei echten Pflanzen,
        unter realen Bedingungen.
      </p>
    </div>

    <div class="tile tile--dark">
      <ContactTile />
    </div>
  </section>
</Layout>

<style>
  .tiles {
    display: grid;
    grid-template-columns: 1fr 1fr;
    max-width: 1200px;
    margin-inline: auto;
  }

  .tile {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 50vh;
    padding: var(--space-2xl) var(--space-3xl);
    color: var(--color-text);
    background: var(--color-background);
    border-bottom: 1px solid var(--color-border);
  }

  .tile:nth-child(odd) {
    border-right: 1px solid var(--color-border);
  }

  .tile--primary { background: var(--color-primary); color: #FFFFFF; border-color: rgba(0, 0, 0, 0.08); }
  .tile--alt { background: var(--color-background-alt); }
  .tile--secondary { background: var(--color-secondary); color: var(--color-text); border-color: rgba(0, 0, 0, 0.1); }
  .tile--dark { background: var(--color-text); color: var(--color-text-inverse); border-color: rgba(255, 255, 255, 0.1); }

  .tile__icon { width: 48px; height: 48px; margin-bottom: var(--space-lg); opacity: 0.7; }
  .tile h1 { font-family: var(--font-family-heading); font-size: var(--font-size-3xl); line-height: var(--line-height-tight); margin-bottom: var(--space-md); }
  .tile__subtitle { font-size: var(--font-size-md); opacity: 0.75; }
  .tile h2 { font-family: var(--font-family-heading); font-size: var(--font-size-2xl); margin-bottom: var(--space-md); }
  .tile p { color: var(--color-text-light); max-width: 45ch; }
  .tile--primary p, .tile--primary .tile__subtitle { color: rgba(255, 255, 255, 0.8); }
  .tile--dark p { color: rgba(234, 239, 239, 0.75); }

  .tile__dl { margin-top: var(--space-lg); }
  .tile__dl dt { font-weight: 700; margin-top: var(--space-md); }
  .tile__dl dt:first-child { margin-top: 0; }
  .tile__dl dd { margin: 0; opacity: 0.8; font-size: var(--font-size-sm); }

  .back { font-size: var(--font-size-sm); color: rgba(255, 255, 255, 0.7); text-decoration: none; margin-bottom: var(--space-xl); }
  .back:hover { color: #FFFFFF; }

  @media (max-width: 639px) {
    .tiles { grid-template-columns: 1fr; }
    .tile { min-height: 45vh; padding: var(--space-xl) var(--space-lg); border-right: none; }
    .tile:nth-child(odd) { border-right: none; }
    .tile h1 { font-size: var(--font-size-2xl); }
  }
</style>
